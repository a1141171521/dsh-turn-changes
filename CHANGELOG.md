# 版本历史

## 1.0.0 — 静态化首发

把在会话里跑通并逐项验证过的动态插件（v13）固化成可安装的静态插件，并补齐验证与文档。

- **宿主半**：订阅 `tools/result` / `session/event` 采集本回合的文件改动，自建行级 diff 引擎
  （前后缀裁剪 + LCS + 3 行上下文 + 折叠），落盘 `$DSH_HOME/turn-changes`，暴露
  `/turn-changes-api/{poll,get,review,diag}`。
- **客户端半**：`conversation.chat.turnTail` 上的「已改动 N 个文件 +A −M」卡片；
  `details` 座位上的审查面板（统一/分栏视图、折叠、加宽、逐文件定位）。
- **diff 视图**：Cindy 同款配色（整行红/绿底 + 上下文行不上色）、分栏配对、
  词级 emphasis 高亮（四项门槛与区间合并规则照搬）。
- **验证**：`node tools/verify-all.mjs` —— 静态宿主半端到端 21 项、行级 diff 引擎 4012 例对拍、
  分栏配对/词级高亮（真实 rows）、计数预测器。
- **文档**：README + 交接文档 `HANDOVER.md` + 完整技术日志 `docs/06-实现-回合变更卡.md`。

已知未合入：右栏「工具详情」页签、自动打开右栏、「撤销」。

## 动态版历史（v1–v18，在会话内迭代，未单独发版）

对应关系：静态 1.0.0 = 动态 v13。技术细节与实测结论都记在
`docs/06-实现-回合变更卡.md`。

| 版本 | 做了什么 |
|---|---|
| v1–v5 | 动态工具/命令采集改动、卡片雏形、审查面板雏形（多次改口径） |
| v6 | **改为自建行级 diff 引擎**，不再依赖平台 hunk 计数；确立 `additions − deletions ≡ 行数差` 不变量 |
| v7–v10 | 右栏面板接入 `details` 座位；沙箱写入问题定位（绝对路径被拒） |
| v11 | **落盘持久化**：重启后历史卡片仍在 |
| v12 | diff 配色对齐 Cindy（整行底色、明暗两套色值） |
| v13 | **分栏按 Cindy 配对** + **词级高亮**（当前基线） |
| v14–v18 | 探索「把出厂工具详情接回右栏」：实测出座位声明的四条规则（见交接文档第 4.2 节），确认 `renderSlot` 路线不可行，改用手工渲染占用者组件并自检通过；同时**因为重写注册函数丢了卡片注册**，导致卡片消失 |
| 回退 | 回到 v13，卡片恢复；v14–v18 的探索结论保留在文档里，代码作为 1.0 之后的待办项 |
